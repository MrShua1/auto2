# LibTV CLI Module

Internal Auto module containing the complete local LibTV command manual, model
schema notes, node-type references, examples and installation scripts.

Read `SOURCE-SKILL.md` and local command documentation as needed, but call the
installed `libtv` executable directly. Do not invoke the original external Skill.
Live `libtv --help` and fetched model schema override bundled examples.
