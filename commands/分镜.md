---
description: Execute Auto's complete pre-video workflow with bundled tools and original material integration.
agent: build
---

Load the auto skill and its Auto D portability/material rules. Treat this command as
the /分集 pre-video mode unless the user explicitly narrows the requested scope:

$ARGUMENTS

Require the complete script and actual required tools. Missing direct_image_run is
a real blocker for paid image work: install the bundled runtime, restart OpenCode,
then check tool availability, rather than bypassing it through a separate API client.
TSC is performed by the Agent under modules/tsc/MODULE.md, not an external generator.
Use supplied material references in actual scenes and props. Do not create previews
or promote every shot to a new SEG. Preserve every approval and final-validation gate.
Do not generate video.
